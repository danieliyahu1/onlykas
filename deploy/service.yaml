apiVersion: v1
kind: Service
metadata:
  name: onlykas
  namespace: onlykas
  labels:
    app.kubernetes.io/name: onlykas
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: onlykas
  ports:
    - name: http
      port: 80
      targetPort: http
