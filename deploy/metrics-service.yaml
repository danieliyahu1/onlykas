apiVersion: v1
kind: Service
metadata:
  name: onlykas-metrics
  namespace: onlykas
  labels:
    app.kubernetes.io/name: onlykas
    app.kubernetes.io/component: metrics
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: onlykas
    app.kubernetes.io/component: web
  ports:
    - name: metrics
      port: 9090
      targetPort: metrics
